# Default values for supersimplechart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: neilghosh/hello-spring
  tag: latest

service:
  type: ClusterIP
  port: 80


